This app uses the create-react-app template system.
For some React and CSS practice, I've decided to create a small image gallery app.

After installing dependencies, type 'npm start' to run this app.